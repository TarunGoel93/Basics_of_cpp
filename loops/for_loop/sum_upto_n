#include<iostream>
using namespace std;

int main()
{
    int sum=0,i,num;
    
    cout<<"Enter the last number : ";
    cin>>num;
    
    for(i=num;i>=1;i--){
        sum+=i;
    }
    
    cout<<"Sum : "<<sum<<endl;
    
return 0;
}
