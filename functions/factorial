#include<iostream>
using namespace std;

int factorial(int num,int fact=1)
{
    for(int i=num;i>=1;i--){
        fact*=i;
    }
    return fact;
}

int main()
{
    
    int x;
    
    cout<<"Enter the number : ";
    cin>>x;
    
    cout<<"Factorial : "<<factorial(x)<<endl;
    
return 0;
}

